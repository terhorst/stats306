install.packages <- function(pkg="packagename", ...) {
    stop(paste0("install.packages() is not necessary on jupyter.stats306.org. Use library('", pkg, "') instead."))
}
options(jupyter.plot_mimetypes="image/png",
        jupyter.display_mimetypes = "text/plain",
        repr.plot.width = 4, 
        repr.plot.height = 3,
        max.print = 1000)
